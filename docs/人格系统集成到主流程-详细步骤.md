# 人格系统集成到主流程 - 详细步骤

## 概述

本文档详细说明如何将隐式反馈系统（CodeOverrideDetector、EMA Manager、Rollback Manager）集成到 Ox CLI 的主循环中。

**设计原则**: 
- ✅ 最小侵入 - 只修改必要的地方
- ✅ 向后兼容 - 不影响现有功能
- ✅ 渐进式集成 - 分步骤验证

---

## 已完成的工作

### ✅ Step 1: App 结构扩展

**文件**: `crates/ox-cli/src/terminal/app.rs`

已添加字段：
```rust
pub struct App {
    // ... existing fields ...
    
    // Implicit feedback system
    pub override_detector: ox_core::feedback::CodeOverrideDetector,
    pub ema_manager: ox_core::feedback::Emamanager,
    pub rollback_manager: ox_core::feedback::RollbackManager,
    
    // Tracking counters for implicit feedback
    pub total_file_writes: u32,
    pub accepted_file_writes: u32,
    pub explicit_feedback_count: u32,
    pub good_feedback_count: u32,
}
```

已更新初始化：
```rust
impl App {
    pub fn new() -> Self {
        Self {
            // ... existing initialization ...
            
            override_detector: CodeOverrideDetector::new(300), // 5 min window
            ema_manager: Emamanager::new(0.2), // alpha = 0.2
            rollback_manager: RollbackManager::new(10), // max 10 snapshots per lang
            total_file_writes: 0,
            accepted_file_writes: 0,
            explicit_feedback_count: 0,
            good_feedback_count: 0,
        }
    }
}
```

**状态**: ✅ 已完成

---

## 待完成的集成步骤

### 🔧 Step 2: 在 run_app 中加载 EMA 历史状态

**位置**: `crates/ox-cli/src/main.rs` - `run_app()` 函数

**时机**: 在创建 `persona_vector` 后，主循环前

**代码**:
```rust
// After line ~430 where persona_vector is created
let mut persona_vector = ox_core::persona::PersonaVector::for_language(...);

// Load EMA historical state from database
if let Err(e) = app.ema_manager.load_from_store("code_accept_rate", &memory.overall_store()) {
    tracing::warn!("Failed to load EMA history: {}", e);
}

// Load baseline satisfaction (could be stored in config or DB)
let baseline_satisfaction = config.feedback.as_ref()
    .map(|f| f.baseline_satisfaction)
    .unwrap_or(0.75); // Default baseline
```

**配置扩展** (可选):
```toml
# In config.toml
[feedback]
baseline_satisfaction = 0.75
detection_window_secs = 300
ema_alpha = 0.2
max_snapshots_per_lang = 10
```

---

### 🔧 Step 3: 注册文件写入事件

**位置**: `crates/ox-cli/src/main.rs` - `AgentToUiEvent::ToolResult` 处理

**当前代码** (line ~862):
```rust
AgentToUiEvent::ToolResult { name, output, is_error } => {
    let summary = summarize_tool_result(&name, &output);
    app.output.push_line(OutputLine::ToolResult {
        name: name.clone(),
        summary,
        is_error,
    });
    if !app.user_scrolled { app.scroll_to_bottom(); }
    app.dirty = true;
}
```

**修改为**:
```rust
AgentToUiEvent::ToolResult { name, output, is_error } => {
    let summary = summarize_tool_result(&name, &output);
    app.output.push_line(OutputLine::ToolResult {
        name: name.clone(),
        summary,
        is_error,
    });
    
    // Register file writes for implicit feedback tracking
    if name == "file_write" && !is_error {
        // Extract file path and content from output
        // Output format: "Wrote N bytes to /path/to/file"
        if let Some(path_str) = extract_file_path_from_output(&output) {
            if let Ok(path) = std::path::PathBuf::from(path_str).canonicalize() {
                // Extract content from the tool call arguments (stored in session)
                if let Some(content) = extract_last_file_write_content(&target_session.messages) {
                    app.override_detector.register_write(path.clone(), &content);
                    app.total_file_writes += 1;
                    
                    tracing::debug!(
                        "[IMPLICIT FEEDBACK] Registered write: {:?}, total: {}",
                        path,
                        app.total_file_writes
                    );
                }
            }
        }
    }
    
    if !app.user_scrolled { app.scroll_to_bottom(); }
    app.dirty = true;
}
```

**辅助函数** (添加到 main.rs 顶部):
```rust
/// Extract file path from file_write output
fn extract_file_path_from_output(output: &str) -> Option<String> {
    // Output format: "Wrote 1234 bytes to /path/to/file"
    if let Some(pos) = output.find("to ") {
        let path_part = &output[pos + 3..];
        Some(path_part.trim().to_string())
    } else {
        None
    }
}

/// Extract content from last file_write tool call in messages
fn extract_last_file_write_content(messages: &[Message]) -> Option<String> {
    // Search backwards for the last Assistant message with file_write tool call
    for msg in messages.iter().rev() {
        if let Message::Assistant { tool_calls, .. } = msg {
            for tc in tool_calls {
                if tc.name == "file_write" {
                    // Parse arguments JSON to extract content
                    if let Ok(args) = serde_json::from_str::<serde_json::Value>(&tc.arguments) {
                        return args.get("content")
                            .and_then(|c| c.as_str())
                            .map(|s| s.to_string());
                    }
                }
            }
        }
    }
    None
}
```

---

### 🔧 Step 4: 用户输入前检测改动

**位置**: `crates/ox-cli/src/main.rs` - 主循环开头或 `handle_key_event` 之前

**最佳位置**: 在主循环的 `tokio::select!` 之前

**代码**:
```rust
loop {
    // === IMPLICIT FEEDBACK: Detect overrides before user input ===
    let override_signals = app.override_detector.detect_overrides();
    
    for signal in &override_signals {
        use ox_core::feedback::{map_override_to_feedback, ImplicitFeedback};
        
        if let Some(feedback) = map_override_to_feedback(signal.change_ratio) {
            match feedback {
                ImplicitFeedback::WeakNegative => {
                    // Minor change (5-30%) - don't count as rejection
                    tracing::debug!(
                        "[IMPLICIT FEEDBACK] Minor change: {:?} ({:.1}%)",
                        signal.path,
                        signal.change_ratio * 100.0
                    );
                }
                ImplicitFeedback::StrongNegative => {
                    // Major rewrite (>30%) - count as rejection
                    tracing::info!(
                        "[IMPLICIT FEEDBACK] Major rewrite: {:?} ({:.1}%)",
                        signal.path,
                        signal.change_ratio * 100.0
                    );
                    // Don't increment accepted_file_writes
                }
                ImplicitFeedback::VeryStrongNegative => {
                    // File deleted - very strong negative
                    tracing::warn!(
                        "[IMPLICIT FEEDBACK] File deleted: {:?}",
                        signal.path
                    );
                    // Don't increment accepted_file_writes
                }
            }
        } else {
            // No significant change (<5%) - count as acceptance
            app.accepted_file_writes += 1;
            tracing::debug!(
                "[IMPLICIT FEEDBACK] Accepted: {:?} (change: {:.1}%)",
                signal.path,
                signal.change_ratio * 100.0
            );
        }
    }
    
    // Update EMA tracker with current accept_rate
    if app.total_file_writes > 0 {
        let accept_rate = app.ema_manager.calculate_accept_rate(
            app.total_file_writes,
            app.accepted_file_writes,
        );
        
        // Persist EMA state periodically (every 10 writes)
        if app.total_file_writes % 10 == 0 {
            let store_clone = memory.overall_store().clone();
            let metric_name = "code_accept_rate".to_string();
            let ema_clone = app.ema_manager.clone();
            
            tokio::spawn(async move {
                if let Err(e) = ema_clone.persist_to_store(&metric_name, &store_clone) {
                    tracing::warn!("Failed to persist EMA state: {}", e);
                }
            });
        }
        
        tracing::debug!(
            "[FEEDBACK METRICS] accept_rate={:.2}, total={}, accepted={}",
            accept_rate,
            app.total_file_writes,
            app.accepted_file_writes
        );
    }
    
    // === END IMPLICIT FEEDBACK DETECTION ===
    
    // Only re-render when needed (dirty or spinner animation changed).
    if app.needs_render() {
        terminal.draw(|frame| render::render(frame, &mut app, tick_count))?;
        app.dirty = false;
        app.mark_spinner_rendered();
    }
    
    // ... rest of the loop ...
}
```

---

### 🔧 Step 5: TurnDone 事件中评估回滚

**位置**: `crates/ox-cli/src/main.rs` - `AgentToUiEvent::TurnDone` 处理

**当前代码** (line ~912-927):
```rust
// Passive persona evolution based on memory patterns
if config.persona.auto_evolve && !persona_vector.frozen {
    let evolution_log = memory.run_self_evaluation(
        &mut persona_vector,
        config.persona.max_trait_change,
    );
    if !evolution_log.is_empty() {
        for log_entry in &evolution_log {
            tracing::info!("[PERSONA EVOLUTION] {}", log_entry);
        }
        app.output.push_line(OutputLine::System(format!(
            "Persona evolved: {} adjustments",
            evolution_log.len()
        )));
    }
}
```

**修改为**:
```rust
// === IMPLICIT FEEDBACK: Evaluate satisfaction and maybe rollback ===
if config.persona.auto_evolve && !persona_vector.frozen {
    // Calculate composite satisfaction score
    let explicit_rate = if app.explicit_feedback_count > 0 {
        app.good_feedback_count as f64 / app.explicit_feedback_count as f64
    } else {
        0.5 // Neutral if no explicit feedback
    };
    
    // Get tool success rate (you may need to track this separately)
    let tool_success_rate = calculate_tool_success_rate(&target_session.messages);
    
    // Get code accept rate from EMA
    let code_accept_rate = app.ema_manager.get_value("code_accept_rate")
        .unwrap_or(0.8); // Default if not tracked yet
    
    let has_explicit = app.explicit_feedback_count >= 5;
    
    let satisfaction_score = app.rollback_manager.calculate_satisfaction_score(
        explicit_rate,
        tool_success_rate,
        code_accept_rate,
        has_explicit,
    );
    
    tracing::info!(
        "[SATISFACTION EVAL] explicit={:.2}, tool={:.2}, code_accept={:.2}, overall={:.2}",
        explicit_rate,
        tool_success_rate,
        code_accept_rate,
        satisfaction_score
    );
    
    // Evaluate and maybe rollback
    match app.rollback_manager.evaluate_and_maybe_rollback(
        &mut persona_vector,
        satisfaction_score,
        baseline_satisfaction,
        memory.overall_store(),
    ) {
        Ok(decision) => {
            use ox_core::feedback::RollbackDecision;
            
            match decision {
                RollbackDecision::RolledBack { from_score, to_score, snapshot_id } => {
                    app.output.push_system(&format!(
                        "⚠️  Persona rolled back due to satisfaction drop ({:.2} → {:.2})",
                        from_score, to_score
                    ));
                    tracing::warn!(
                        "[ROLLBACK] Snapshot: {}, Score: {:.2} → {:.2}",
                        snapshot_id, from_score, to_score
                    );
                }
                RollbackDecision::NeedsRollback { current_score, baseline_score, degradation } => {
                    app.output.push_system(&format!(
                        "⚠️  Warning: Satisfaction dropped ({:.2} < {:.2}), but no rollback point available",
                        current_score, baseline_score
                    ));
                    tracing::warn!(
                        "[ROLLBACK NEEDED] Current: {:.2}, Baseline: {:.2}, Degradation: {:.2}",
                        current_score, baseline_score, degradation
                    );
                }
                RollbackDecision::NoRollback { current_score } => {
                    // Normal evolution can proceed
                    let evolution_log = memory.run_self_evaluation(
                        &mut persona_vector,
                        config.persona.max_trait_change,
                    );
                    
                    if !evolution_log.is_empty() {
                        for log_entry in &evolution_log {
                            tracing::info!("[PERSONA EVOLUTION] {}", log_entry);
                        }
                        app.output.push_line(OutputLine::System(format!(
                            "✨ Persona evolved: {} adjustments (satisfaction: {:.2})",
                            evolution_log.len(),
                            current_score
                        )));
                        
                        // Create snapshot after successful evolution
                        if let Err(e) = app.rollback_manager.create_snapshot(
                            &persona_vector,
                            current_score,
                            memory.overall_store(),
                        ) {
                            tracing::warn!("Failed to create persona snapshot: {}", e);
                        }
                    }
                }
            }
        }
        Err(e) => {
            tracing::error!("Rollback evaluation failed: {}", e);
            // Fallback to normal evolution
            let evolution_log = memory.run_self_evaluation(
                &mut persona_vector,
                config.persona.max_trait_change,
            );
            // ... handle evolution log ...
        }
    }
} else if !config.persona.auto_evolve {
    app.output.push_system("Auto-evolve is disabled. Enable it in config.toml.");
}
// === END IMPLICIT FEEDBACK ROLLBACK EVALUATION ===
```

**辅助函数** (添加到 main.rs):
```rust
/// Calculate tool success rate from session messages
fn calculate_tool_success_rate(messages: &[Message]) -> f64 {
    let mut total_tools = 0u32;
    let mut successful_tools = 0u32;
    
    for msg in messages {
        if let Message::ToolResult { is_error, .. } = msg {
            total_tools += 1;
            if !is_error {
                successful_tools += 1;
            }
        }
    }
    
    if total_tools == 0 {
        1.0 // No tools used, assume perfect
    } else {
        successful_tools as f64 / total_tools as f64
    }
}
```

---

### 🔧 Step 6: 显式反馈处理

**位置**: `crates/ox-cli/src/main.rs` - `SlashCommand::Feedback` 处理

**查找位置**: 搜索 `SlashCommand::Feedback`

**当前代码**:
```rust
SlashCommand::Feedback { kind } => {
    match kind.as_str() {
        "good" => {
            // Reinforce memories
            // ...
            app.output.push_system("Feedback noted: positive");
        }
        "bad" => {
            persona_vector.process_feedback("bad", config.persona.max_trait_change);
            app.output.push_system("Feedback noted: negative. Persona adjusted.");
        }
        _ => {
            app.output.push_system("Usage: /feedback <good|bad|unsafe>");
        }
    }
}
```

**修改为**:
```rust
SlashCommand::Feedback { kind } => {
    match kind.as_str() {
        "good" => {
            app.good_feedback_count += 1;
            app.explicit_feedback_count += 1;
            
            // Reinforce memories (existing logic)
            let recent_user_msg = session.messages.iter().rev()
                .find_map(|m| match m {
                    Message::User { content } => Some(content.clone()),
                    _ => None,
                });
            
            if let Some(query) = recent_user_msg {
                let memory_nodes = memory.retrieve(&query, &Some(rt_env.project_id.as_str()), 5);
                let accessed_ids: Vec<&str> = memory_nodes.iter().map(|n| n.id.as_str()).collect();
                memory.reinforce_accessed(&accessed_ids);
            }
            
            app.output.push_system("✅ Feedback noted: positive");
            tracing::info!(
                "[EXPLICIT FEEDBACK] Good: {}/{}",
                app.good_feedback_count,
                app.explicit_feedback_count
            );
        }
        "bad" => {
            app.explicit_feedback_count += 1;
            // Don't increment good_feedback_count
            
            // Process feedback (existing logic)
            persona_vector.process_feedback("bad", config.persona.max_trait_change);
            
            app.output.push_system("⚠️  Feedback noted: negative. Persona adjusted.");
            tracing::info!(
                "[EXPLICIT FEEDBACK] Bad: {}/{}",
                app.good_feedback_count,
                app.explicit_feedback_count
            );
        }
        "unsafe" => {
            // Safety violations are logged but refuses_unsafe_code cannot be changed
            app.output.push_system("🔒 Safety violation noted. Reviewing constraints.");
            tracing::warn!("[SAFETY VIOLATION] Reported by user");
        }
        _ => {
            app.output.push_system("Usage: /feedback <good|bad|unsafe>");
        }
    }
}
```

---

## 测试验证

### 编译检查
```bash
cargo check --package ox-cli
```

### 功能测试

1. **测试文件写入注册**:
   ```
   /write test.txt Hello World
   # Check logs for: [IMPLICIT FEEDBACK] Registered write
   ```

2. **测试改动检测**:
   ```
   # Manually edit test.txt significantly
   # Then type anything in Ox
   # Check logs for: [IMPLICIT FEEDBACK] Major rewrite
   ```

3. **测试满意度评估**:
   ```
   /feedback good
   /feedback bad
   # Wait for next turn completion
   # Check logs for: [SATISFACTION EVAL]
   ```

4. **测试回滚机制**:
   ```
   # Trigger multiple bad feedbacks
   /feedback bad
   /feedback bad
   /feedback bad
   # Check if rollback occurs
   ```

---

## 配置建议

在 `config.toml` 中添加:

```toml
[feedback]
enabled = true
baseline_satisfaction = 0.75
detection_window_secs = 300
ema_alpha = 0.2
max_snapshots_per_lang = 10
rollback_threshold = 0.2
log_level = "info"  # debug, info, warn
```

---

## 监控和调试

### 关键日志

```bash
# Watch implicit feedback events
tail -f ~/.ox/logs/ox.log | grep "IMPLICIT FEEDBACK"

# Watch satisfaction evaluation
tail -f ~/.ox/logs/ox.log | grep "SATISFACTION EVAL"

# Watch rollback decisions
tail -f ~/.ox/logs/ox.log | grep "ROLLBACK"
```

### 数据库查询

```sql
-- Check EMA trends
SELECT * FROM ema_trends WHERE metric_name = 'code_accept_rate';

-- Check persona snapshots
SELECT snapshot_id, language, satisfaction_score, created_at 
FROM persona_snapshots 
ORDER BY created_at DESC 
LIMIT 10;

-- Check active snapshot
SELECT * FROM persona_snapshots WHERE is_active = 1;
```

---

## 性能影响

| 操作 | 耗时 | 频率 | 总开销 |
|------|------|------|--------|
| 检测改动 | ~5ms | 每次用户输入前 | < 1% CPU |
| EMA 更新 | <1μs | 每次用户输入前 | 可忽略 |
| SQLite 持久化 | ~1-5ms | 每 10 次写入 | 异步，不阻塞 |
| 回滚评估 | ~2ms | 每次对话结束 | < 0.1% CPU |

**结论**: 性能开销极小，用户无感知。

---

## 常见问题

### Q1: 为什么不在每次文件写入后立即检测改动？

**A**: 用户可能还在编辑中，立即检测会产生误报。等待用户下一次输入时检测更准确。

### Q2: 如果 EMA 状态丢失怎么办？

**A**: 从默认值重新开始。EMA 会快速收敛（~10-20 个样本）。

### Q3: 回滚会不会导致无限循环？

**A**: 不会。回滚后创建了新的快照，下次评估基于新基线。且有 `max_snapshots_per_lang` 限制。

### Q4: 如何禁用隐式反馈？

**A**: 在 config.toml 中设置 `feedback.enabled = false`，或在代码中条件判断。

---

## 总结

集成步骤总结：
1. ✅ App 结构扩展（已完成）
2. 🔧 加载 EMA 历史状态
3. 🔧 注册文件写入事件
4. 🔧 用户输入前检测改动
5. 🔧 TurnDone 中评估回滚
6. 🔧 显式反馈计数更新

**预期工作量**: 约 2-3 小时  
**风险等级**: 低（模块化设计，易于回退）  
**收益**: 自动适应用户偏好，减少手动反馈需求
